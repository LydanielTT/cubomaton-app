<!--<h2>Acquisitions principales</h2>-->
<!--<div class="container">-->
<!--  <div class="row" *ngFor="let stone of stones" [routerLink]="['/detail', hero.id]>-->
<!--  <div class="row" *ngFor="let stone of stones">-->
<!--    <div class="card card-block">-->
<!--      <h4 class="card-title">{{ stone.name }}</h4>-->
<!--      <p class="card-text">{{ stone.body }}</p>-->
<!--      <a href="#" class="card-link">Card link</a>-->
<!--      <a href="#" class="card-link">Another link</a>-->
<!--    </div>-->
<!--  </div>-->
<!--</div>-->
<h2>Echantillons</h2>
<div>
  <label>name:</label> <input #heroName />
  <button (click)="add(heroName.value); heroName.value=''" class="addstone">+</button>
</div>

<button >+</button>
<ul class="stones">
  <li *ngFor="let stone of stones" [class.selected]="stone === selectedStone" (click)="onSelect(stone)">
    <span class="badge">{{ stone.id }}</span>
    <span>{{ stone.name }}</span>
    <span class="queue">{{ stone.updateAt }}</span>
<!--    <button class="delete" (click)="delete(hero); $event.stopPropagation()">X</button>-->
  </li>
</ul>
<!-- my-hero-detail [hero]="selectedHero"></my-hero-detail-->
<div *ngIf="selectedStone">
  <h2>
    {{ selectedStone.name | uppercase }} est selectionné
  </h2>
  <button (click)="gotoDetail()">View Details</button>
</div>
